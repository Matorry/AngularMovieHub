import { BudgetFormatPipe } from './budget.format.pipe';

describe('BudgetFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BudgetFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
